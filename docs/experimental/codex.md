# Codex support (moved)

Codex support is no longer experimental. This page has moved to [docs/codex.md](../codex.md).

Enable it with `leiter codex install` and disable it with `leiter codex uninstall`. The config key is now `codex`; the
old `enable_codex_experimental` name is still accepted for backward compatibility.
